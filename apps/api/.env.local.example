# ClawSentinel API — Local Environment Variables
# ─────────────────────────────────────────────────────────────────────────────
# HOW TO USE:
#   1. Copy this file:  cp .env.local.example .env.local
#   2. Fill in your actual values in .env.local
#   3. Never commit .env.local — it is already in .gitignore
#   4. Never share .env.local contents in chat or commits
# ─────────────────────────────────────────────────────────────────────────────

# ── STRIPE (Test Mode) ────────────────────────────────────────────────────────
# Get these from: https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=sk_test_REPLACE_ME
STRIPE_PUBLISHABLE_KEY=pk_test_REPLACE_ME

# Get this after creating the webhook in Stripe Dashboard:
# Stripe Dashboard → Developers → Webhooks → Add endpoint
# Endpoint URL: http://localhost:3001/api/webhook  (for local testing with Stripe CLI)
# Events to listen: checkout.session.completed, customer.subscription.deleted, invoice.payment_failed
STRIPE_WEBHOOK_SECRET=whsec_REPLACE_ME

# Get this after creating the product in Stripe:
# Stripe Dashboard → Products → Create → "ClawSentinel Pro" → $9/month → copy Price ID
STRIPE_PRICE_ID=price_REPLACE_ME

# ── JWT SIGNING ───────────────────────────────────────────────────────────────
# Generate a strong random secret (run this in terminal to generate one):
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# Never reuse this value anywhere else
JWT_SECRET=REPLACE_ME_WITH_64_BYTE_HEX

# ── EMAIL (for sending activation keys) ──────────────────────────────────────
# Resend is recommended (free tier: 3000 emails/month): https://resend.com
RESEND_API_KEY=re_REPLACE_ME
EMAIL_FROM=noreply@clawsentinel.dev

# ── DATABASE ──────────────────────────────────────────────────────────────────
# For local dev: uses a local SQLite file (no setup needed)
# For production (Vercel): replace with Vercel KV or Postgres URL
DATABASE_URL=file:./dev.db

# ── APP ───────────────────────────────────────────────────────────────────────
NODE_ENV=development
API_BASE_URL=http://localhost:3001
