{
  "name": "@clawsentinel/cli",
  "version": "0.4.0",
  "description": "ClawSentinel CLI — manage and monitor all 5 security modules",
  "bin": {
    "clawsentinel": "./dist/index.cjs"
  },
  "main": "./dist/index.cjs",
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts --clean --banner.js '#!/usr/bin/env node'",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@clawsentinel/core": "*",
    "@clawsentinel/clawvault": "*",
    "@clawsentinel/clawhub-scanner": "*",
    "commander": "^12.0.0",
    "chalk": "^5.3.0",
    "ora": "^8.0.1"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsup": "^8.0.0",
    "tsx": "^4.7.0",
    "vitest": "^1.4.0"
  }
}
