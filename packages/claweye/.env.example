# ClawEye — Environment Variables
# Copy to .env.local for Next.js local development.

# Port the dashboard runs on
# Default: 7432
# CLAWEYE_PORT=7432

# Shared SQLite database path (must match ClawGuard's CLAWSENTINEL_DB)
# Default: ~/.clawsentinel/clawsentinel.db
# CLAWSENTINEL_DB=~/.clawsentinel/clawsentinel.db

# ClawGuard health endpoint (used by status API for module liveness checks)
# Default: http://localhost:18791
# CLAWGUARD_HEALTH_URL=http://localhost:18791

# SSE poll interval in milliseconds (how often the event stream polls SQLite)
# Default: 2000
# SSE_POLL_INTERVAL=2000
