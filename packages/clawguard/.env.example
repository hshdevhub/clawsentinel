# ClawGuard — Environment Variables
# Copy to .env for local development.

# Port this proxy listens on
# Default: 18790
# LISTEN_PORT=18790

# Upstream WebSocket URL (where OpenClaw actually runs)
# Default: ws://127.0.0.1:18789
# UPSTREAM_WS=ws://127.0.0.1:18789

# Upstream HTTP URL
# Default: http://127.0.0.1:18789
# UPSTREAM_HTTP=http://127.0.0.1:18789

# Shared SQLite database path
# Default: ~/.clawsentinel/clawsentinel.db
# CLAWSENTINEL_DB=~/.clawsentinel/clawsentinel.db

# Semantic engine: set to 'off' to force pattern-only mode (no LLM calls)
# Values: auto | off
# Default: auto (uses first available key from ClawVault)
# SEMANTIC_ENGINE=auto

# Score threshold above which the semantic engine is consulted
# Default: 30
# SEMANTIC_THRESHOLD=30

# Log level: error | warn | info | debug
# Default: info
# LOG_LEVEL=info
